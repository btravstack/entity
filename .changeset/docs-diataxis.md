---
"@btravstack/entity": patch
---

Documentation restructured along Diátaxis lines. The README was a 764-line
design document serving two different readers badly; it is now a ~140-line
landing page with one worked example covering the whole lifecycle, and the
rest lives in `docs/` split by what the reader is trying to do: `reference.md`
for lookup, `explanation.md` for rationale, and four how-to guides. The
npm-facing package README becomes a short card pointing at them.
